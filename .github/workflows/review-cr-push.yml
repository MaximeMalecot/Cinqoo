name: Review-ms build and push docker image

on:
  pull_request:
    types:
      - opened
    branches:
      - master
    paths:
      - review-service/**

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:  
      - name: Checkout
        uses: actions/checkout@v2
      - name: echo
        run: echo "REVIEW MS"